interface UserOption {
    value: string;
    label: string;
  }

interface StatusOption {
    value: string;
    label: string;
}

interface SalerOption {
    value: string;
    label: string;
}
interface CustomerOption {
    value: string;
    label: string;
}
export type { UserOption, StatusOption, SalerOption, CustomerOption }  